%h1 Editing <%= singular_name %>

- labelled_form_for(@<%= singular_name %>) do |f|
  = render :partial => "<%= plural_name %>/form", :object => f

= link_to t(:"actions.show"), @<%= singular_name %> |
= link_to t(:"actions.back"), <%= plural_name %>_path

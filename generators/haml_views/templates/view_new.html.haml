%h1
  New <%= singular_name %>

- form_for(@<%= singular_name %>) do |f|
  = render :partial => "<%= plural_name %>/form", :object => f
  %p= f.submit "Create"

= link_to 'Back', <%= plural_name %>_path
